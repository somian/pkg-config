From 6253e3619ab70ae62a7e12490178303a25b415c3 Mon Sep 17 00:00:00 2001
From: Jasper Lievisse Adriaanse <jasper@humppa.nl>
Date: Sun, 6 Mar 2011 20:52:20 +0100
Subject: [PATCH 03/14] - document several other honored environment variables.

---
 pkg-config.1 |   31 +++++++++++++++++++++++++++++++
 1 files changed, 31 insertions(+), 0 deletions(-)

diff --git a/pkg-config.1 b/pkg-config.1
index 75db7c9..71c6b03 100644
--- a/pkg-config.1
+++ b/pkg-config.1
@@ -128,12 +128,43 @@ If set, this will cause
 .Nm
 to print various debugging information.
 .El
+.Bl -tag -width PKG_CONFIG_DISABLE_UNINSTALLED
+.It Ev PKG_CONFIG_DISABLE_UNINSTALLED
+By default
+.Nm
+prefers a package named "foo-uninstalled" over "foo" when the package
+"foo" is requested.
+This allows to link/compile against uninstalled packages.
+Setting this flag disables the default behaviour.
+.El
+.Bl -tag -width PKG_CONFIG_LIBDIR
+.It Ev PKG_CONFIG_LIBDIR
+This can be used to specify a colon-separated list of paths to search for
+package files.
+This will replace the default
+.Nm
+search directory.
+.El
+.Bl -tag -width PKG_CONFIG_LOGFILE
+.It Ev PKG_CONFIG_LOGFILE
+This is used to specify a logfile to which
+.Nm
+will write the passed arguments.
+.El
 .Bl -tag -width PKG_CONFIG_PATH
 .It Ev PKG_CONFIG_PATH
 This can be used to specify a colon-separated list of paths to search for
 package files.
 If given, this list of paths is prepended to the standard search path.
 .El
+.Bl -tag -width PKG_CONFIG_TOP_BUILD_DIR
+.It Ev PKG_CONFIG_TOP_BUILD_DIR
+If set
+.Nm
+uses the specified value for
+.Em pc_top_builddir
+instead of $(top_builddir).
+.El
 .Sh EXIT STATUS
 .Ex -std pkg-config
 .Sh SEE ALSO
-- 
1.7.3.5

